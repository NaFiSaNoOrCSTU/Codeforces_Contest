#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while(t--)
    {
        int n,m,x,y;
        cin >> n >> m >> x >> y;
        int t,t1;
        t=n;
        t1=m;
        while(n--)
        {
            int a;
            cin >> a;
        }
        while(m--)
        {
            int b;
            cin >> b;
        }
        cout << t+t1 << endl;
    }
    return 0;
}